import React from 'react'
import { getEggspressSettings } from '../utils'

const authorLinks = () => {
  const blogSettings = getEggspressSettings

  return (
    <div>authorLinks</div>
  )
}

export default authorLinks